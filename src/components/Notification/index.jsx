import React from 'react';

const Notification = () => (
  <span>
    <b>CHARACTER NOT ALLOWED</b>
    <br />
    (only letters, numbers, hyphens and spaces)
  </span>
);

export default Notification;
